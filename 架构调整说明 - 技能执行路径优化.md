# 🔄 架构调整说明

## 问题发现

**错误信息**：
```
[ReadRequirement] 错误：此技能需要在 Node.js 环境中运行
[SkillExecutor] ❌ 技能执行失败：read_requirement 
ReferenceError: process is not defined
```

**根本原因**：
- 前端应用（React/Vite）运行在**浏览器环境**
- `ReadRequirementSkill` 需要**Node.js 环境**才能读取文件系统
- 前端直接在前端执行技能会导致"process is not defined"错误

## 架构调整

### 原方案（方案三）的问题

**设计**：
```
前端 TaskPlanner
  ↓
SkillExecutor（前端）
  ↓
ReadRequirementSkill（浏览器中执行）❌ 失败
```

**问题**：
- ❌ 前端浏览器无法访问文件系统
- ❌ 前端 SkillExecutor 无法执行需要 Node.js 环境的技能

### 调整后的架构

**设计**：
```
前端 TaskPlanner
  ↓
SkillsAPIClient
  ↓
HTTP POST /api/skills/execute
  ↓
后端 server/routes/skills.js
  ↓
后端 SkillExecutor（Node.js 环境）
  ↓
ReadRequirementSkill ✅ 成功
```

**优势**：
- ✅ 前端只负责 UI 和任务规划
- ✅ 后端负责执行需要 Node.js 环境的技能
- ✅ 前后端职责分离
- ✅ 符合 Web 应用架构最佳实践

## 修改内容

### 1. TaskPlanner 执行逻辑

**文件**：`src/lib/taskPlanner.js`

**修改前**：
```javascript
import { skillExecutor } from '../skills/core/SkillExecutor.js';

// 在前端直接执行
if (skillExecutor.hasSkill(step.action)) {
  const result = await skillExecutor.execute(step.action, step.params);
}
```

**修改后**：
```javascript
import { skillsAPIClient } from '../skills/SkillsAPIClient.js';

// 通过 API 调用后端执行
if (step.action) {
  const result = await skillsAPIClient.executeSkill(step.action, 'execute', step.params);
}
```

### 2. 后端 API 执行逻辑

**文件**：`server/routes/skills.js`

**已有实现**：
```javascript
router.post('/execute', async (req, res) => {
  const { skillName, action, params } = req.body;
  
  // 使用 SkillExecutor 执行技能
  const { skillExecutor } = await import('../../src/skills/core/SkillExecutor.js');
  
  if (!skillExecutor.hasSkill(skillName)) {
    return res.status(404).json({ error: 'Skill not found' });
  }
  
  const result = await skillExecutor.execute(skillName, params || {});
  
  res.json({
    success: true,
    message: `Skill "${skillName}" executed successfully`,
    result
  });
});
```

### 3. 前端 SkillExecutor 的新角色

**前端 SkillExecutor** 现在主要用于：
1. **TerminalAgent**：在 Node.js 环境中执行技能（如果 TerminalAgent 在 Node.js 中运行）
2. **技能注册中心**：作为技能注册的参考
3. **调试工具**：查询技能状态

**实际执行路径**：
- 前端 UI → TaskPlanner → SkillsAPIClient → 后端 API → 后端 SkillExecutor → 技能实例

## 技能执行流程

### 完整流程

```
1. 用户在终端输入："读取需求文档 F:\...\智慧客房 APP 项目需求.md"
   ↓
2. AI 助手识别意图，生成任务
   ↓
3. TaskPlanner 分析任务，生成步骤
   - 步骤 1: read_requirement (path: "F:\...")
   ↓
4. TaskPlanner 执行步骤
   ↓
5. skillsAPIClient.executeSkill('read_requirement', 'execute', params)
   ↓
6. HTTP POST http://localhost:3003/api/skills/execute
   Body: { skillName: 'read_requirement', params: {...} }
   ↓
7. 后端 /api/skills/execute 路由处理
   ↓
8. 后端 SkillExecutor.execute('read_requirement', params)
   ↓
9. ReadRequirementSkill.execute(params)
   ↓
10. Node.js fs.readFile() 读取文件
    ↓
11. 返回结果给前端
    ↓
12. 前端显示结果
```

### 错误处理

```javascript
// TaskPlanner 中的错误处理
try {
  const result = await skillsAPIClient.executeSkill(step.action, 'execute', step.params);
  // 处理成功结果
} catch (error) {
  // 处理错误
  console.error(`[TaskPlanner] ❌ 步骤 ${step.name} 执行失败:`, error);
  results.push({
    step: step.step,
    name: step.name,
    status: 'failed',
    error: error.message
  });
}
```

## 前后端 SkillExecutor 对比

| 特性 | 前端 SkillExecutor | 后端 SkillExecutor |
|------|------------------|------------------|
| 运行环境 | 浏览器 | Node.js |
| 技能注册 | ✅ 可以注册 | ✅ 已注册 |
| 执行文件操作 | ❌ 不支持 | ✅ 支持 |
| 执行网络请求 | ✅ 支持 | ✅ 支持 |
| 主要用途 | TerminalAgent、调试 | 实际技能执行 |
| TaskPlanner 使用 | ❌ 不使用 | ✅ 通过 API 使用 |

## 部署说明

### 启动顺序

1. **启动后端服务**
   ```bash
   cd f:\openclaw\commander-pro
   npm run server
   ```
   
   后端会：
   - 启动 Express 服务器（端口 3003）
   - 注册技能到后端 SkillExecutor
   - 监听 /api/skills/execute 请求

2. **启动前端应用**
   ```bash
   cd f:\openclaw\commander-pro
   npm run dev
   ```
   
   前端会：
   - 启动 Vite 开发服务器（端口 3004）
   - 注册技能到前端 SkillExecutor（用于 TerminalAgent）
   - 通过 SkillsAPIClient 调用后端 API

### 验证方法

**1. 检查后端服务**
```bash
curl http://localhost:3003/health
# 应返回：{"status":"ok",...}
```

**2. 检查技能执行**
```bash
curl -X POST http://localhost:3003/api/skills/execute \
  -H "Content-Type: application/json" \
  -d '{"skillName":"read_requirement","params":{"path":"F:\\openclaw\\projects\\HarmonyOS\\HarmonyOS_Demo\\docs\\智慧客房 APP 项目需求.md"}}'
```

**3. 在前端测试**
```
在 Commander Pro 终端输入：
读取"F:\openclaw\projects\HarmonyOS\HarmonyOS_Demo\docs\智慧客房 APP 项目需求.md"
```

## 调试技巧

### 前端日志

```javascript
// 在浏览器控制台查看
[TaskPlanner] 🚀 通过后端 API 执行技能：read_requirement
[SkillsAPIClient] Executing skill: read_requirement
```

### 后端日志

```javascript
// 在后端控制台查看
[Skills API] 🚀 使用 SkillExecutor 执行技能：read_requirement
[SkillExecutor] 🚀 执行技能：read_requirement
[ReadRequirement] 尝试读取文件：F:\...
[SkillExecutor] ✅ 技能执行成功：read_requirement
```

### 常见问题

**Q1: 技能执行失败 "process is not defined"**
- 原因：前端直接执行了需要 Node.js 环境的技能
- 解决：确保 TaskPlanner 使用 skillsAPIClient 调用后端

**Q2: 后端返回 "Skill not found"**
- 原因：后端 SkillExecutor 未注册该技能
- 解决：检查 server/index.js 中是否注册了技能

**Q3: HTTP 请求失败**
- 原因：后端服务未启动
- 解决：运行 `npm run server` 启动后端

## 架构优势

### 1. 职责分离 ✅
- **前端**：UI、任务规划、用户交互
- **后端**：文件操作、系统调用、技能执行

### 2. 安全性 ✅
- 前端无法直接访问文件系统
- 所有敏感操作都在后端执行
- 可以在后端进行安全验证

### 3. 可维护性 ✅
- 技能实现集中在后端
- 前端不需要关心技能如何执行
- 易于添加新技能

### 4. 可扩展性 ✅
- 后端可以部署到独立服务器
- 前端可以部署到 CDN
- 支持负载均衡

## 下一步优化建议

### 短期（本次上线）
- [x] 修改 TaskPlanner 使用 SkillsAPIClient
- [x] 确保后端正确执行技能
- [ ] 全面测试所有技能
- [ ] 添加错误处理和日志

### 中期
- [ ] 添加技能执行缓存
- [ ] 优化后端技能加载
- [ ] 添加技能执行超时控制
- [ ] 添加技能执行进度反馈

### 长期
- [ ] 支持技能并行执行
- [ ] 支持技能执行队列
- [ ] 支持技能执行历史记录
- [ ] 支持技能执行统计分析

---

*架构调整完成时间：2026-05-19*
*调整原因：前端浏览器环境无法执行文件操作技能*
