# 读取需求文档技能问题总结

## 问题现象

当用户请求"根据需求文档生成项目"时，任务规划系统尝试读取需求文档，但失败了。

## 根本原因

1. **技能执行环境问题**：
   - 技能在浏览器环境中被调用，而不是 Node.js 环境
   - `isNode` 检测失败，导致 `fs` 模块未加载

2. **路径处理问题**：
   - 中文路径在 Node.js 中的处理需要特殊注意
   - 但测试表明 Node.js 本身支持中文路径

## 已创建的解决方案

### 1. 创建专门的读取技能

文件：`src/skills/file/ReadRequirement.js`

特点：
- 专门用于读取需求文档
- 支持中文路径
- 限制只能读取 `projects` 目录下的 `.md` 文件
- 安全性检查

### 2. 注册技能

在 `src/skills/LazySkillLoader.js` 中注册：
```javascript
'read_requirement': () => import('./file/ReadRequirement.js'),
```

## 测试方法

### 直接测试技能

```bash
cd f:\openclaw\commander-pro
node test-read-requirement.js
```

### 在应用中使用

在终端输入：
```
根据"F:\openclaw\projects\HarmonyOS\HarmonyOS_Demo\docs\智慧客房 APP 项目需求.md"生成项目
```

## 当前状态

✅ 技能已创建  
✅ 技能已注册  
✅ 测试脚本已创建  
⚠️ 需要验证在真实环境中是否工作  

## 下一步

1. **验证技能在真实环境中工作**：
   - 在 Commander Pro 终端中测试读取需求文档
   - 检查是否能在 Node.js 环境中正确执行

2. **修改任务规划器**：
   - 使用 `read_requirement` 技能代替 `safe_read_file`
   - 确保技能在正确的环境中执行

3. **测试完整流程**：
   - 读取需求文档
   - AI 分析需求
   - 生成项目代码

## 备选方案

如果技能仍然无法在浏览器环境中工作，可以：

1. **预加载需求文档**：
   - 在应用启动时读取常用需求文档
   - 缓存在内存中供 AI 使用

2. **使用后端 API**：
   - 创建专门的 API 端点读取文件
   - 前端通过 HTTP 请求获取内容

3. **手动上传**：
   - 让用户通过文件选择器上传需求文档
   - 读取后传递给 AI

## 文件清单

- `src/skills/file/ReadRequirement.js` - 需求文档读取技能
- `test-read-requirement.js` - 技能测试脚本
- `test-chinese-path.js` - 中文路径测试脚本

---

*创建时间：2026-05-15*
