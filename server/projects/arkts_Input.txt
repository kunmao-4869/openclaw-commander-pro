# 代码示例：arkts-common-components-text-input-V5

> 来源：https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/arkts-common-components-text-input-V5
> 提取时间：2026/4/14 11:19:44

---

## 创建输入框 - TextInput为单行输入框、TextArea为多行输入框。通过以下接口来创建。 1

**语言**: typescript

```typescript
TextInput(value?:{placeholder?: ResourceStr, text?: ResourceStr, controller?: TextInputController})
```

## 创建输入框 - TextInput为单行输入框、TextArea为多行输入框。通过以下接口来创建。 2

**语言**: typescript

```typescript
TextArea(value?:{placeholder?: ResourceStr, text?: ResourceStr, controller?: TextAreaController})
```

## 创建输入框 - TextInput为单行输入框、TextArea为多行输入框。通过以下接口来创建。 3

**语言**: typescript

```typescript
TextInput()
```

## 创建输入框 - TextInput为单行输入框、TextArea为多行输入框。通过以下接口来创建。 4

**语言**: typescript

```typescript
TextArea()
```

## 创建输入框 - TextInput为单行输入框、TextArea为多行输入框。通过以下接口来创建。 5

**语言**: typescript

```typescript
TextArea({ text: "我是TextArea我是TextArea我是TextArea我是TextArea" }).width(300)
```

## 设置输入框类型 - TextInput有以下类型可选择：Normal基本输入模式、Password密码输入模式、Email邮箱地址输入模式、Number纯数字输入模式、PhoneNumber电话号码输入模式、USER_NAME用户名输入模式、NEW_PASSWORD新密码输入模式、NUMBER_PA... 6

**语言**: typescript

```typescript
TextInput()  .type(InputType.Normal)
```

## 设置输入框类型 - TextInput有以下类型可选择：Normal基本输入模式、Password密码输入模式、Email邮箱地址输入模式、Number纯数字输入模式、PhoneNumber电话号码输入模式、USER_NAME用户名输入模式、NEW_PASSWORD新密码输入模式、NUMBER_PA... 7

**语言**: typescript

```typescript
TextInput()  .type(InputType.Password)
```

## 设置输入框类型 - TextInput有以下类型可选择：Normal基本输入模式、Password密码输入模式、Email邮箱地址输入模式、Number纯数字输入模式、PhoneNumber电话号码输入模式、USER_NAME用户名输入模式、NEW_PASSWORD新密码输入模式、NUMBER_PA... 8

**语言**: typescript

```typescript
TextInput()  .type(InputType.Email)
```

## 设置输入框类型 - TextInput有以下类型可选择：Normal基本输入模式、Password密码输入模式、Email邮箱地址输入模式、Number纯数字输入模式、PhoneNumber电话号码输入模式、USER_NAME用户名输入模式、NEW_PASSWORD新密码输入模式、NUMBER_PA... 9

**语言**: typescript

```typescript
TextInput()  .type(InputType.Number)
```

## 设置输入框类型 - TextInput有以下类型可选择：Normal基本输入模式、Password密码输入模式、Email邮箱地址输入模式、Number纯数字输入模式、PhoneNumber电话号码输入模式、USER_NAME用户名输入模式、NEW_PASSWORD新密码输入模式、NUMBER_PA... 10

**语言**: typescript

```typescript
TextInput()  .type(InputType.PhoneNumber)
```

## 设置输入框类型 - TextInput有以下类型可选择：Normal基本输入模式、Password密码输入模式、Email邮箱地址输入模式、Number纯数字输入模式、PhoneNumber电话号码输入模式、USER_NAME用户名输入模式、NEW_PASSWORD新密码输入模式、NUMBER_PA... 11

**语言**: typescript

```typescript
TextInput()  .type(InputType.NUMBER_DECIMAL)
```

## 设置输入框类型 - TextInput有以下类型可选择：Normal基本输入模式、Password密码输入模式、Email邮箱地址输入模式、Number纯数字输入模式、PhoneNumber电话号码输入模式、USER_NAME用户名输入模式、NEW_PASSWORD新密码输入模式、NUMBER_PA... 12

**语言**: typescript

```typescript
TextInput()  .type(InputType.URL)
```

## 自定义样式 - 设置无输入时的提示文本。 13

**语言**: typescript

```typescript
TextInput({ placeholder: '我是提示文本' })
```

## 自定义样式 - 设置无输入时的提示文本。 14

**语言**: typescript

```typescript
TextInput({ placeholder: '我是提示文本', text: '我是当前文本内容' })
```

## 自定义样式 - 设置无输入时的提示文本。 15

**语言**: typescript

```typescript
TextInput({ placeholder: '我是提示文本', text: '我是当前文本内容' })  .backgroundColor(Color.Pink)
```

## 添加事件 - 文本框主要用于获取用户输入的信息，把信息处理成数据进行上传，绑定onChange事件可以获取输入框内改变的内容。用户也可以使用通用事件来进行相应的交互操作。 16

**语言**: typescript

```typescript
TextInput()  .onChange((value: string) => {    console.info(value);  })  .onFocus(() => {    console.info('获取焦点');  })
```

## 场景示例 - 在登录/注册页面，用户进行登录或注册。 17

**语言**: typescript

```typescript
@Entry@Componentstruct TextInputSample {  build() {    Column() {      TextInput({ placeholder: 'input your username' }).margin({ top: 20 })        .onSubmit((EnterKeyType) => {          console.info(EnterKeyType + '输入法回车键的类型值');        })      TextInput({ placeholder: 'input your password' }).type(InputType.Password).margin({ top: 20 })        .onSubmit((EnterKeyType) => {          console.info(EnterKeyType + '输入法回车键的类型值');        })      Button('Sign in').width(150).margin({ top: 20 })    }.padding(20)  }}
```

## 键盘避让 - 键盘抬起后，具有滚动能力的容器组件在横竖屏切换时，才会生效键盘避让，若希望无滚动能力的容器组件也生效键盘避让，建议在组件外嵌套一层具有滚动能力的容器组件，比如Scroll、List、Grid。 18

**语言**: typescript

```typescript
// xxx.ets@Entry@Componentstruct Index {  placeHolderArr: string[] = ['1', '2', '3', '4', '5', '6', '7'];
  build() {    Scroll() {      Column() {        ForEach(this.placeHolderArr, (placeholder: string) => {          TextInput({ placeholder: 'TextInput ' + placeholder })            .margin(30)        })      }    }    .height('100%')    .width('100%')  }}
```

