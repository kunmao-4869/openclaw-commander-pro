# OpenClaw Commander Pro 使用教程

## 📖 目录

1. [简介](#简介)
2. [快速开始](#快速开始)
3. [界面介绍](#界面介绍)
4. [节点类型](#节点类型)
5. [技能库](#技能库)
6. [工作流示例](#工作流示例)
7. [高级功能](#高级功能)
8. [常见问题](#常见问题)

---

## 🎯 简介

**OpenClaw Commander Pro** 是一个强大的可视化工作流自动化编辑器，让您无需编写代码即可创建复杂的自动化任务。

### 核心功能
- 🖱️ **可视化编辑** - 拖拽式界面，直观易用
- 🔗 **节点连接** - 自由组合各种功能节点
- 🤖 **技能系统** - 12+ 预制技能，覆盖多种场景
- ⚡ **实时执行** - 一键运行工作流，即时查看结果
- 💾 **保存加载** - 支持工作流的保存和导入

---

## 🚀 快速开始

### 1. 启动服务

```bash
# 终端 1: 启动后端服务
cd commander-pro
npm run server

# 终端 2: 启动开发服务器
cd commander-pro
npm run dev

# 终端 3: 启动终端服务（可选）
cd commander-pro
node server/terminal.js
```

### 2. 访问应用

打开浏览器访问：**http://localhost:3001**

### 3. 创建第一个工作流

#### 步骤 1：添加开始节点
- 点击左侧工具栏的 **"开始"** 按钮（绿色）
- 或在画布上右键选择添加节点

#### 步骤 2：添加技能节点
- 点击左侧工具栏的 **"技能"** 按钮（蓝色）
- 拖动节点到合适位置

#### 步骤 3：连接节点
- 从"开始"节点的**右侧圆点**按下鼠标
- 拖动到"技能"节点的**左侧圆点**
- 松开鼠标，完成连接

#### 步骤 4：配置技能
- 点击技能节点
- 在右侧属性面板选择技能（如"启动应用"）
- 配置参数（如应用名称）
- 点击保存按钮 💾

#### 步骤 5：运行工作流
- 点击右上角的绿色 **"运行"** 按钮
- 打开浏览器控制台（F12）查看执行日志

---

## 🖥️ 界面介绍

### 整体布局

```
┌─────────────────────────────────────────────────────────────┐
│  顶部工具栏：运行 | 保存 | 打开 | 缩放控制                   │
├──────────┬──────────────────────────────────────┬───────────┤
│ 左侧工具 │           中央画布区域                 │ 右侧属性  │
│ 栏       │                                      │ 面板      │
│          │  [开始] ──→ [技能] ──→ [结束]         │           │
│ - 开始   │                                      │ - 节点名  │
│ - 技能   │         网格背景                      │ - 描述    │
│ - 条件   │                                      │ - 技能选择│
│ - 结束   │                                      │ - 参数配置│
│ - 鼠标   │                                      │           │
│ - 键盘   │                                      │           │
├──────────┴──────────────────────────────────────┴───────────┤
│  底部状态栏：内存使用 | 对话数量 | 当前模型                   │
└─────────────────────────────────────────────────────────────┘
```

### 工具栏功能

| 按钮 | 功能 | 说明 |
|------|------|------|
| ▶️ 运行 | 执行工作流 | 从开始节点顺序执行 |
| 💾 保存 | 保存工作流 | 导出为 JSON 文件 |
| 📂 打开 | 加载工作流 | 导入 JSON 文件 |
| ➕ 添加 | 添加节点 | 快速添加节点到画布 |
| ⚙️ 设置 | 系统设置 | 配置系统参数 |

---

## 📦 节点类型

### 1. 🟢 开始节点（Start）
- **颜色**：绿色
- **功能**：工作流的起点
- **连接**：只有输出（右侧）
- **说明**：每个工作流必须有一个开始节点

### 2. 🔵 技能节点（Skill）
- **颜色**：蓝色
- **功能**：执行特定技能
- **连接**：输入 + 输出
- **配置**：
  - 选择技能（12 种可选）
  - 配置技能参数
  - 添加自定义参数

### 3. 🟡 条件节点（Condition）
- **颜色**：黄色
- **功能**：条件判断，分支执行
- **连接**：输入 + 多个输出
- **配置**：
  - 条件表达式（JavaScript 语法）
  - 示例：`result.success === true`

### 4. 🔴 结束节点（End）
- **颜色**：红色
- **功能**：工作流终点
- **连接**：只有输入（左侧）
- **说明**：可选，建议添加

### 5. 🟣 鼠标移动节点（Mouse）
- **颜色**：紫色
- **功能**：控制鼠标移动和点击
- **连接**：输入 + 输出
- **配置**：
  - X 坐标：目标 X 位置
  - Y 坐标：目标 Y 位置
  - 点击类型：左键/右键/中键/不点击

### 6. 🩷 键盘输入节点（Keyboard）
- **颜色**：粉色
- **功能**：模拟键盘输入文本
- **连接**：输入 + 输出
- **配置**：
  - 输入文本：要输入的内容
  - 按键延迟：每个按键的间隔（毫秒）

---

## 🛠️ 技能库

### 浏览器类

#### 🔍 浏览器搜索 (browser_search)
**描述**：使用真实浏览器搜索，绕过 API 限制

**参数**：
```json
{
  "query": "搜索关键词",
  "engine": "bing"  // 或 "google"、"baidu"
}
```

**使用场景**：需要大量搜索结果时

---

#### 🌐 网络搜索 (web_search)
**描述**：快速网络搜索（DuckDuckGo API）

**参数**：
```json
{
  "query": "搜索关键词",
  "limit": 10
}
```

**使用场景**：快速获取简单信息

---

### AI 类

#### 🤖 分析搜索结果 (analyze_search_results)
**描述**：AI 分析搜索结果，提取关键信息

**参数**：
```json
{
  "maxResults": 5,
  "summaryLength": "medium"
}
```

---

#### ⚖️ 对比来源 (compare_sources)
**描述**：对比多个信息来源的可靠性

**参数**：
```json
{
  "sources": ["url1", "url2"],
  "criteria": "权威性"
}
```

---

### 代码类

#### 💻 生成项目代码 (generate_project_code)
**描述**：根据需求生成项目代码

**参数**：
```json
{
  "description": "项目描述",
  "language": "javascript",
  "framework": "react"
}
```

---

#### 🔎 代码审查 (review_code)
**描述**：审查代码质量和安全性

**参数**：
```json
{
  "code": "代码内容",
  "language": "javascript",
  "strictness": "high"
}
```

---

### 文件类

#### 📄 读取文件 (safe_read_file)
**描述**：安全读取文件内容

**参数**：
```json
{
  "filePath": "/path/to/file.txt",
  "encoding": "utf-8"
}
```

---

#### ✍️ 写入文件 (safe_write_file)
**描述**：安全写入文件

**参数**：
```json
{
  "filePath": "/path/to/output.txt",
  "content": "文件内容",
  "encoding": "utf-8"
}
```

---

### 系统类

#### 🚀 启动应用 (launch_application)
**描述**：启动指定的应用程序

**参数**：
```json
{
  "appName": "应用名称或关键词",
  "args": ["启动参数 1", "启动参数 2"]
}
```

**支持的应用**：
- 网易云音乐
- 抖音
- 哔哩哔哩
- Microsoft Edge
- QQ
- 微信
- IntelliJ IDEA
- DevEco Studio
- Trae CN
- 豆包

---

#### 💻 系统信息 (get_system_info)
**描述**：获取系统信息（CPU、内存等）

**参数**：无

---

### 自动化类

#### 🖱️ 移动鼠标 (mouse_move)
**描述**：移动鼠标到指定坐标并点击

**参数**：
```json
{
  "x": 100,
  "y": 200,
  "click": "left"  // 或 "right"、"middle"、"none"
}
```

---

#### ⌨️ 键盘输入 (keyboard_input)
**描述**：模拟键盘输入文本

**参数**：
```json
{
  "text": "要输入的文本",
  "delay": 50  // 毫秒
}
```

---

## 📚 工作流示例

### 示例 1：自动启动应用

```
[开始] → [启动应用：微信] → [结束]
```

**步骤**：
1. 添加开始节点
2. 添加技能节点，选择"启动应用"
3. 配置参数：`appName: "微信"`
4. 添加结束节点
5. 连接所有节点
6. 点击运行

---

### 示例 2：搜索并保存结果

```
[开始] 
  ↓
[网络搜索] query="AI 新闻"
  ↓
[写入文件] filePath="news.txt"
  ↓
[结束]
```

**配置**：
- 网络搜索节点：
  ```json
  { "query": "AI 新闻", "limit": 10 }
  ```
- 写入文件节点：
  ```json
  { "filePath": "news.txt", "content": "${searchResults}" }
  ```

---

### 示例 3：条件分支

```
[开始]
  ↓
[网络搜索] query="TypeScript 教程"
  ↓
[条件] result.length > 0
  ├─ true → [写入文件] → [结束]
  └─ false → [键盘输入"未找到结果"] → [结束]
```

**条件节点配置**：
```javascript
result.length > 0
```

---

### 示例 4：自动化操作

```
[开始]
  ↓
[启动应用] appName="edge"
  ↓
[移动鼠标] x=500, y=300, click="left"
  ↓
[键盘输入] text="https://github.com"
  ↓
[移动鼠标] x=800, y=400, click="left"
  ↓
[结束]
```

---

### 示例 5：完整工作流

```
[开始]
  ↓
[浏览器搜索] query="OpenClaw 教程"
  ↓
[分析搜索结果]
  ↓
[条件] summary.includes("教程")
  ├─ true → [写入文件] filePath="tutorial.txt" → [结束]
  └─ false → [键盘输入"未找到教程"] → [结束]
```

---

## ⚡ 高级功能

### 1. 工作流保存/加载

#### 保存工作流
1. 点击右上角的 **"保存"** 按钮
2. 选择保存位置
3. 输入文件名（如 `my-workflow.json`）
4. 确认保存

#### 加载工作流
1. 点击右上角的 **"打开"** 按钮
2. 选择之前保存的 JSON 文件
3. 工作流自动加载到画布

---

### 2. 画布操作

#### 缩放
- 使用右下角的缩放控制
- 或使用鼠标滚轮（按住 Ctrl）
- 范围：10% - 500%

#### 平移
- 按住鼠标右键拖动
- 或使用空格 + 左键拖动

#### 选择节点
- 单击节点即可选中
- 选中后节点会高亮显示
- 右侧属性面板自动打开

---

### 3. 节点管理

#### 复制节点
- 点击节点右上角的复制图标 📋
- 或使用快捷键 Ctrl+C / Cmd+C

#### 删除节点
- 点击节点右上角的删除图标 🗑️
- 或选中后按 Delete 键
- 连接线会自动删除

#### 移动节点
- 拖动节点到任意位置
- 连接线会跟随移动

---

### 4. 连接管理

#### 创建连接
1. 从输出连接点（右侧圆点）按下鼠标
2. 拖动到输入连接点（左侧圆点）
3. 松开鼠标

#### 删除连接
1. 点击连接线选中
2. 按 Delete 键
3. 或在属性面板删除

#### 连接线样式
- **临时连线**：黄色虚线（拖拽时）
- **已完成**：浅绿色虚线
- **选中**：白色实线

---

### 5. 调试技巧

#### 查看执行日志
1. 打开浏览器开发者工具（F12）
2. 切换到 Console 标签
3. 运行工作流
4. 查看日志输出：
   - 🚀 开始执行
   - 📍 执行节点
   - 🔧 执行技能
   - ✅ 执行完成
   - ❌ 执行失败

#### 常见错误
- **节点未连接**：确保所有节点都正确连接
- **技能未配置**：检查技能参数是否完整
- **API 失败**：确保后端服务正在运行
- **条件错误**：检查条件表达式语法

---

## ❓ 常见问题

### Q1: 工作流无法运行？
**A**: 检查以下几点：
- ✅ 是否包含"开始"节点
- ✅ 节点是否正确连接
- ✅ 技能参数是否配置完整
- ✅ 后端服务是否运行（端口 3003）

---

### Q2: 技能执行失败？
**A**: 
- 检查网络连接
- 查看浏览器控制台的错误信息
- 确认技能参数格式正确
- 某些技能需要特定权限

---

### Q3: 鼠标/键盘操作无效？
**A**:
- 确保后端服务正在运行
- 检查目标应用是否有焦点
- 坐标是否在屏幕范围内
- 某些应用可能需要管理员权限

---

### Q4: 如何自定义技能？
**A**:
1. 在 `src/skills/` 目录创建新文件
2. 继承 `SecureSkill` 类
3. 实现 `execute` 方法
4. 在 `LazySkillLoader.js` 中注册
5. 在 `PropertiesPanel.jsx` 中添加选项

---

### Q5: 工作流可以嵌套吗？
**A**: 
目前不支持子工作流调用。但可以通过组合多个节点实现复杂逻辑。

---

### Q6: 支持循环吗？
**A**:
目前不支持循环结构。计划在未来版本中添加。

---

### Q7: 如何分享工作流？
**A**:
1. 保存工作流为 JSON 文件
2. 分享 JSON 文件给其他人
3. 对方使用"打开"功能加载

---

## 🎓 最佳实践

### 1. 命名规范
- 给节点起有意义的名称
- 使用描述性的技能参数
- 添加详细的节点描述

### 2. 错误处理
- 使用条件节点检查执行结果
- 添加错误处理分支
- 记录关键步骤的日志

### 3. 性能优化
- 优先使用快速技能（如 web_search）
- 避免不必要的鼠标/键盘操作
- 合理使用延迟参数

### 4. 可维护性
- 保持工作流简洁
- 添加注释说明复杂逻辑
- 定期保存备份

---

## 📞 获取帮助

如果遇到无法解决的问题：
1. 查看浏览器控制台的错误信息
2. 检查后端服务的日志
3. 查阅本文档的相关章节
4. 查看示例工作流

---

## 🎉 结语

恭喜您完成了 OpenClaw Commander Pro 的学习！

现在您可以：
- ✅ 创建各种自动化工作流
- ✅ 使用 12+ 种预制技能
- ✅ 实现复杂的业务逻辑
- ✅ 提高工作效率

**开始创建您的第一个自动化工作流吧！** 🚀

---

*最后更新：2025-01-XX*
*版本：OpenClaw Commander Pro v2.0*
